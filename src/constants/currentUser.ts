// There is no real login system yet, so every page that needs "the current
// user" uses this fixed development id instead (see aqairjo_server/schema.sql,
// users id 1).
export const CURRENT_USER_ID = 1;
