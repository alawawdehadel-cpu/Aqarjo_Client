// Static list of Jordan cities used to populate dropdowns (search bar,
// filters). This is a UI constant, not application data, so it doesn't
// come from the backend.
export const cities = ["Amman", "Irbid", "Aqaba", "Zarqa"];
